# Local dev identity override (Mode A).
# These are used when forwarded headers are missing and no Databricks Apps env vars are set.
DEV_USER=devuser
DEV_EMAIL=devuser@example.com

# Optional: set to mimic Databricks Apps env and disable DEV_* overrides.
# DATABRICKS_HOST=
# DATABRICKS_APP_PORT=8000
