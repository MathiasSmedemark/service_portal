[project]
name = "service-portal-backend"
version = "0.0.0"
description = "Service portal backend"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110.0",
  "uvicorn>=0.29.0",
]

[tool.uv]
package = false

dev-dependencies = [
  "pytest>=7.4.0",
  "httpx>=0.25.0",
]
